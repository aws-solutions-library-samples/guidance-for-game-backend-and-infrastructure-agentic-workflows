"""E5 bounded-autonomy activation/rollback runbook + cost model (Issue #440).

This package holds the operator-facing E5 activation/rollback runbook and a
pure, testable cost model. Nothing here deploys infrastructure, grants provider
write permission, or calls AWS.
"""
