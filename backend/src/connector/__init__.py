"""Source Control Connector package.

Provides the opt-in, fail-closed GitOps write path that lets the agent propose
Infrastructure-as-Code (IaC) changes as pull/merge requests instead of mutating
live AWS resources. See ``.kiro/specs/source-control-connector`` for the design.
"""
